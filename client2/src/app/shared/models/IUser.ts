export interface IUser{
     id?: string
     email?: string,
     imageUrl?: string, 
     displayName?: string,
     token?: string,
     avatarUrl?: string
     image?: string,
     isAdmin?: boolean
}